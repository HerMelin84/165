from sys import argv

def word_counter():
    try:
        argv[1]
        k for i.split() in (for i in argv[1:]:
            lc = 0
            file= open(i, 'r')
            for line in file: 
                lc+=1
            print i + ": " + str(lc)
    except IndexError:
        print "IndexError: No argument given."
        print argv[0]+" <INPUT>"
        print "Example: " + argv[0] + " readme.txt"
    except IOError:
        print "IOError: No such file."
        print "The file you entered did not exist!"
line_counter()

