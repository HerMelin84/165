from sys import argv

def line_counter():
    try:
        i=0;
        line = [for k in xrange(1,len(argv))]
        
    except IndexError:
        print "IndexError: No argument given."
        print argv[0]+" <INPUT>"
        print "Example: " + argv[0] + "75.2"
